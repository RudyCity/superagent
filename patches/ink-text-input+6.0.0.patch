diff --git a/node_modules/ink-text-input/build/index.js b/node_modules/ink-text-input/build/index.js
index ada7cdc..410c6e2 100644
--- a/node_modules/ink-text-input/build/index.js
+++ b/node_modules/ink-text-input/build/index.js
@@ -13,7 +13,7 @@ function TextInput({ value: originalValue, placeholder = '', focus = true, mask,
                 return previousState;
             }
             const newValue = originalValue || '';
-            if (previousState.cursorOffset > newValue.length - 1) {
+            if (previousState.cursorOffset > newValue.length) {
                 return {
                     cursorOffset: newValue.length,
                     cursorWidth: 0,
@@ -72,7 +72,8 @@ function TextInput({ value: originalValue, placeholder = '', focus = true, mask,
                 nextCursorOffset++;
             }
         }
-        else if (key.backspace || key.delete) {
+        else if (key.backspace) {
+            // Backspace: delete character BEFORE the cursor
             if (cursorOffset > 0) {
                 nextValue =
                     originalValue.slice(0, cursorOffset - 1) +
@@ -80,6 +81,15 @@ function TextInput({ value: originalValue, placeholder = '', focus = true, mask,
                 nextCursorOffset--;
             }
         }
+        else if (key.delete) {
+            // Delete (forward-delete): delete character AFTER the cursor
+            if (cursorOffset < originalValue.length) {
+                nextValue =
+                    originalValue.slice(0, cursorOffset) +
+                        originalValue.slice(cursorOffset + 1, originalValue.length);
+                // nextCursorOffset stays the same — cursor doesn't move
+            }
+        }
         else {
             nextValue =
                 originalValue.slice(0, cursorOffset) +
@@ -90,11 +100,11 @@ function TextInput({ value: originalValue, placeholder = '', focus = true, mask,
                 nextCursorWidth = input.length;
             }
         }
-        if (cursorOffset < 0) {
+        if (nextCursorOffset < 0) {
             nextCursorOffset = 0;
         }
-        if (cursorOffset > originalValue.length) {
-            nextCursorOffset = originalValue.length;
+        if (nextCursorOffset > nextValue.length) {
+            nextCursorOffset = nextValue.length;
         }
         setState({
             cursorOffset: nextCursorOffset,
